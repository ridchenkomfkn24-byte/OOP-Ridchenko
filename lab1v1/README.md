# ЗВІТ
### з лабораторної роботи №1
**З дисципліни:** «Об’єктно-орієнтоване програмування»  
**Тема:** Створення першого проєкту. Класи, об’єкти, конструктори та деструктори. Середовище розробки.  
**Студент(ка):** [Ваше Прізвище та Ім’я]  
**Група:** [Назва вашої групи]  
**Варіант:** №1 (Клас `Book`)  

---

## 1. Мета роботи
Ознайомитися з середовищем розробки Visual Studio Code, навчитися створювати класи та працювати з базовими елементами об'єктно-орієнтованого програмування (інкапсуляція, конструктори, деструктори, властивості) у мові C#. Налаштувати середовище розробки та інструменти контролю версій Git.

---

## 2. Теоретичні відомості
* **Клас** — це шаблон для створення об’єктів. Він описує, які дані (поля) та яку поведінку (методи) матимуть його екземпляри.
* **Конструктор** — це спеціальний метод, що автоматично викликається під час створення нового об’єкта (`new ClassName()`). Його основне завдання — ініціалізувати початковий стан об’єкта.
* **Деструктор** — це метод, що викликається перед тим, як об’єкт буде знищено збирачем сміття (Garbage Collector).
* **Інкапсуляція** — це механізм приховування внутрішніх деталей реалізації. Доступ до даних класу контролюється через публічні властивості (`get`, `set`), тоді як самі дані зберігаються у приватних полях.

---

## 3. Хід виконання роботи

1. **Налаштування середовища:** 
   * Установлено Visual Studio Code та необхідні розширення (*C# Dev Kit*, *.NET Install Tool*).
   * Перевірено функціонування .NET SDK у терміналі.

2. **Створення консольного проєкту:** 
   * У терміналі створено новий консольний проєкт у потрібній директорії:
     ```bash
     dotnet new console -o OOP-Shevchenko/lab1v1
     ```

3. **Реалізація класу `Book`:**
   * Створено приватні поля `_title`, `_author`, `_year` (інкапсуляція).
   * Реалізовано публічні властивості `Title`, `Author`, `Year` із виразами-стрілками (`=>`) для доступу до полів.
   * Оголошено параметризований конструктор для ініціалізації об'єктів під час створення.
   * Додано деструктор `~Book()` для відстеження вилучення об'єкта з пам'яті.
   * Написано метод `GetInfo()`, який повертає форматований рядок із відомостями про книгу.

4. **Демонстрація в методі `Main`:**
   * Встановлено кодування `UTF-8` для коректного виведення кирилиці в консоль.
   * Створено 3 екземпляри класу `Book` ("1984", "Інтернат", "Зазираючи у прірву").
   * Викликано метод `GetInfo()` для демонстрації даних кожного об'єкта.
   * Продемонстровано використання властивості `set` шляхом зміни року для третьої книги (`book3.Year = 2024`).

5. **Налаштування Git:**
   * У кореневій директорії проєкту створено файл `.gitignore` (згенерований сервісом Toptal для C#, DotnetCore, VisualStudioCode).
   * Виконано ініціалізацію Git-репозиторію, додано файли, зроблено коміт та опубліковано код на GitHub.